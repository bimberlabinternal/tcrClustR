% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/DirichletDistances.R
\name{ExtractGroupDistanceVectors}
\alias{ExtractGroupDistanceVectors}
\title{Extract Within-Group TCR Distance Vectors}
\usage{
ExtractGroupDistanceVectors(
  seuratObj,
  assayName,
  splitField,
  minClonesPerGroup = 2,
  verbose = TRUE
)
}
\arguments{
\item{seuratObj}{A Seurat object produced by \code{CalculateTcrDistances}
(must have \code{@misc$TCR_Distances}).}

\item{assayName}{Character. The distance assay to use (e.g., \code{"TRA_fl"},
\code{"TRB_cdr3"}, \code{"TRA_TRB_fl"}).}

\item{splitField}{Character. A metadata column name whose levels define the
groups.}

\item{minClonesPerGroup}{Integer. Minimum number of unique clones required in
a group to produce a distance vector. Default 2.}

\item{verbose}{Logical. Print progress messages. Default TRUE.}
}
\value{
A named list of numeric vectors, one per group level.
}
\description{
For each level of \code{splitField} in the Seurat metadata, maps
cells to their clone indices, subsets the clone-level distance matrix, and
extracts the upper triangle as a flat numeric vector.
}
