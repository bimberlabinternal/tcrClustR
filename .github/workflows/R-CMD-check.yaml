
name: R Build and Checks

on:
  workflow_dispatch:
  push:
    branches: [ main, dev ]
  pull_request:
  schedule:
    - cron: "0 6 10 * *"

jobs:
  # Build base image monthly (uses shared workflow from dockerDependencies)
  build-base:
    uses: GWMcElfresh/dockerDependencies/.github/workflows/build-base-image.yml@main
    permissions:
      contents: read
      packages: write
    secrets: inherit
  
  # Build and test with Docker caching (uses shared workflow from dockerDependencies)
  docker-build-and-test:
    needs: build-base
    if: always()  # Run even if base build is skipped (already exists)
    uses: GWMcElfresh/dockerDependencies/.github/workflows/docker-cache.yml@main
    with:
      r-version: '4.4.2'
      bioc-version: '3.20'
      python-version: '3.8.10'
      use-base-image: true
      test-command: |
        cd /tcrClustR && \
        Rscript -e "devtools::test()" && \
        R CMD build . && \
        R CMD check *.tar.gz --no-manual
      push-images: ${{ github.ref == 'refs/heads/main' }}
      push-runtime: true
    permissions:
      contents: read
      packages: write
    secrets: inherit
